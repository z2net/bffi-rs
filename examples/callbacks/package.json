{
  "name": "bffi-example-callbacks",
  "version": "0.1.0",
  "private": true,
  "description": "Example bffi-rs native module: the callback surface (both directions, the JS-thread gate, marshal delivery) consumed through the @z2net/bffi pipeline. Dependencies come from the package registries (npm: @z2net/bffi, crates.io: bffi).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test"
  },
  "dependencies": {
    "@z2net/bffi": "^0.1.0"
  }
}
